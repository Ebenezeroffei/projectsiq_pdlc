import VehicleFormPageWrapper from "@/components/admin/vehicles/VehicleFormPageWrapper";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Vehicle Form"
}

export default VehicleFormPageWrapper;